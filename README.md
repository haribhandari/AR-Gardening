Prerequisites - Node JS, Visual studio Code, Expo-Cli, Yarn

1. Clone the repo
2. npm i or npm install to install node modules. Alternatively, you can run yarn install 
3. expo start || npm start || yarn start to run the project. 
